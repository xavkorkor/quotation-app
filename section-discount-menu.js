// Alan's United Auto - keep section discount hidden inside the section ⋮ menu.
(function(){
  let busy=false;
  function injectStyle(){if(document.getElementById('auaSectionDiscountMenuStyles'))return;const s=document.createElement('style');s.id='auaSectionDiscountMenuStyles';s.textContent=`.section-menu-pop .aua-section-discount-wrap{border-top:1px solid #e2e8f0;margin-top:4px;padding-top:8px}.section-menu-pop .aua-section-discount-label{font-size:10.5px;font-weight:800;color:#475569;margin-bottom:6px;text-transform:uppercase}.section-menu-pop .aua-section-discount-wrap .disc{display:grid;grid-template-columns:64px minmax(110px,1fr);gap:7px;margin-top:0}.section-menu-pop .aua-section-discount-wrap select,.section-menu-pop .aua-section-discount-wrap input{font-size:12px;padding:7px}`;document.head.appendChild(s)}
  function closeMenu(el){const menu=el?.closest('.section-menu');if(menu)menu.open=false}
  function bindClose(wrap){if(wrap.dataset.closeBound==='1')return;wrap.dataset.closeBound='1';const input=wrap.querySelector('input');if(!input)return;
    input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();input.blur();closeMenu(input)}});
    input.addEventListener('change',()=>setTimeout(()=>closeMenu(input),0));
    input.addEventListener('blur',()=>setTimeout(()=>closeMenu(input),80));
  }
  function moveDiscount(card){if(!card)return;const body=card.querySelector(':scope > .section-body'),pop=card.querySelector('.section-head .section-menu-pop');if(!body||!pop)return;let wrap=pop.querySelector('.aua-section-discount-wrap');if(wrap){bindClose(wrap);return}const children=[...body.children],label=children.find(el=>el.classList?.contains('small')&&/section discount/i.test(el.textContent||''));if(!label)return;const disc=label.nextElementSibling;if(!disc||!disc.classList.contains('disc'))return;wrap=document.createElement('div');wrap.className='aua-section-discount-wrap';const title=document.createElement('div');title.className='aua-section-discount-label';title.textContent='Section Discount — Optional';wrap.appendChild(title);wrap.appendChild(disc);pop.appendChild(wrap);label.remove();bindClose(wrap)}
  function apply(){if(busy)return;busy=true;requestAnimationFrame(()=>{document.querySelectorAll('.section-card').forEach(moveDiscount);busy=false})}
  function install(){injectStyle();apply();const target=document.getElementById('sections')||document.body;new MutationObserver(apply).observe(target,{childList:true,subtree:true});document.addEventListener('pointerdown',e=>{document.querySelectorAll('.section-menu[open]').forEach(menu=>{if(!menu.contains(e.target))menu.open=false})},true)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(install,360));else setTimeout(install,360);
})();